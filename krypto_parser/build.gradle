version '0.0.1-SNAPSHOT'

apply plugin: "application"

dependencies {
    compile project(":krypto_service")
}

mainClassName = "tdlib.Example"

jar {
    manifest.attributes("Main-Class": mainClassName)
}
